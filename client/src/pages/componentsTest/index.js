import { Button } from '@mui/material';

const ComponentsPage = () => {
	return (
		<div>
			<Button variant="contained" className="rounded-full bg-pink-500">
				àdadsfa
			</Button>
		</div>
	);
};

export default ComponentsPage;
